from django.conf.urls import patterns, include, url

urlpatterns = patterns('Job.views',
	url(r'^job_publish/$', 'publish'),
	url(r'^job_detail/(?P<_id>\d+)/$','detail'),
	url(r'^job_delete/(?P<_id>\d+)/$','delete'),
	
)
